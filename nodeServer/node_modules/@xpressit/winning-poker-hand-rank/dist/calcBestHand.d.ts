import { Card } from './card';
import { GameType } from './types';
export declare const calcBestHand: (gameType: GameType, pocketCards: Card[], communityCards: Card[]) => {
    rank: number;
    madeHand: [number, number, number, number, number];
    unused: number[];
};
